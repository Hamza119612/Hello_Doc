{% extends 'base.html.twig' %}

{% block title %}mes RDv{% endblock %}

{% block body %}
{% include "inc/navigation.html.twig" %}

    <h1>Rendezvous Medcin </h1>

    <table class="table">
        <thead>
            <tr> 
                <th>Nom</th>
                <th>prenom</th>
                <th>Telephone</th>
                <th>date_naissance</th>
            </tr>
        </thead>
        <tbody>
        {% for rendezvou in rendezvouses %}
            <tr>

                <td>{{ rendezvou.nom }}</td>
                <td>{{  rendezvou.prenom }}</td>
                <td>{{  rendezvou.telephone }}</td>
                <td>{{  rendezvou.date_de_naissance }}</td>

                {# <td>
                    <a href="{{ path('rendezvous_show', {'id_rdv': rendezvou.id_rdv}) }}">show</a>
                    <a href="{{ path('rendezvous_edit', {'id_rdv': rendezvou.id_rdv}) }}">edit</a>
                </td> #}
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
