{% extends 'base.html.twig' %}

{% block title %}Medcin{% endblock %}

{% block body %}
    <h1>Medcin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medcin.id }}</td>
            </tr>
        
            <tr>
                <th>Adresse</th>
                <td>{{ medcin.adresse }}</td>
            </tr>
            <tr>
                <th>Prix_visite</th>
                <td>{{ medcin.prixVisite }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ medcin.cin }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ medcin.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medcin_index') }}">back to list</a>

    <a href="{{ path('medcin_edit', {'id': medcin.id}) }}">edit</a>

    {{ include('medcin/_delete_form.html.twig') }}
{% endblock %}
