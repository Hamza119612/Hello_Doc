<form method="post" action="{{ path('specialite_delete', {'id_specialite': specialite.id_specialite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialite.id_specialite) }}">
    <button class="btn">Delete</button>
</form>
