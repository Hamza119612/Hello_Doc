{% extends 'base.html.twig' %}

{% block title %}Specialite{% endblock %}

{% block body %}
    <h1>Specialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite.id }}</td>
            </tr>
            <tr>
                <th>Specialite_medcin</th>
                <td>{{ specialite.specialiteMedcin }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ specialite.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('specialite_index') }}">back to list</a>

    <a href="{{ path('specialite_edit', {'id': specialite.id}) }}">edit</a>

    {{ include('specialite/_delete_form.html.twig') }}
{% endblock %}
