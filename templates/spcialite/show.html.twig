{% extends 'base.html.twig' %}

{% block title %}Spcialite{% endblock %}

{% block body %}
    <h1>Spcialite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spcialite.id }}</td>
            </tr>
            <tr>
                <th>Spec_Medcin</th>
                <td>{{ spcialite.SpecMedcin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spcialite_index') }}">back to list</a>

    <a href="{{ path('spcialite_edit', {'id': spcialite.id}) }}">edit</a>

    {{ include('spcialite/_delete_form.html.twig') }}
{% endblock %}
